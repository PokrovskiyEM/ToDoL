# 🎨 Руководство по анимациям для ToDo приложения

## 📋 Содержание
1. [Быстрый старт](#быстрый-старт)
2. [Варианты анимаций](#варианты-анимаций)
3. [Интеграция в проект](#интеграция-в-проект)
4. [Примеры использования](#примеры-использования)

---

## 🚀 Быстрый старт

### Шаг 1: Подключить файл анимаций
Добавьте в `styles/index.css`:
```css
@import "./animations.css";
```

### Шаг 2: Выбрать нужные анимации
Все анимации разделены на блоки. Вы можете удалить ненужные или настроить их под себя.

---

## 🎭 Варианты анимаций

### 1. **Анимация появления задач** ⭐ Рекомендуется
- **Эффект**: Плавное появление с верхнего края со слайдом
- **Когда использовать**: При добавлении новой задачи или загрузке списка
- **Автоматически**: Работает для всех `.todo__item`

**Визуал**: Задачи появляются одна за другой с небольшой задержкой (stagger effect)

---

### 2. **Анимация удаления задач** ⭐ Рекомендуется
- **Эффект**: Скольжение вправо с уменьшением
- **Когда использовать**: При удалении задачи
- **Требует JavaScript**: Нужно добавить класс `.removing` перед удалением

**Пример использования**:
```javascript
const taskElement = document.querySelector(`[data-js-task-id="${taskId}"]`);
taskElement.closest('.todo__item').classList.add('removing');

setTimeout(() => {
  removeToDo(taskId);
}, 300); // Длина анимации
```

---

### 3. **Раскрытие описания задачи** ⭐ Рекомендуется
- **Эффект**: Плавное раскрытие с изменением высоты и прозрачности
- **Когда использовать**: При нажатии на кнопку "..."
- **Автоматически**: Работает для `.todo__item-description`

**Текущая реализация**: Уже интегрировано в ваш код через классы `hidden`/`is-expanded`

---

### 4. **Анимация чекбокса** ⭐ Рекомендуется
- **Эффект**: Появление галочки с вращением и увеличением
- **Когда использовать**: При отметке задачи как выполненной
- **Автоматически**: Работает при `:checked` состоянии

**Бонус**: Пульсация при наведении на неотмеченный чекбокс

---

### 5. **Анимация фильтров** ⭐ Рекомендуется
- **Эффект**: Подчеркивание активного фильтра снизу
- **Когда использовать**: При переключении между фильтрами
- **Автоматически**: Работает для `.is-current`

**Визуал**: Красная линия появляется снизу активной кнопки

---

### 6. **Анимация счетчиков** 
- **Эффект**: Увеличение счетчика при изменении
- **Когда использовать**: При изменении количества задач
- **Требует JavaScript**: Нужно добавить/убрать класс `.no-animation` для первого рендера

**Пример**:
```javascript
// В renderCounters.js
const countElement = document.querySelector('[data-js-tasks-count]');
countElement.classList.add('no-animation'); // Первый рендер без анимации
```

---

### 7. **Анимация пустого состояния**
- **Эффект**: Появление с нижнего края
- **Когда использовать**: При показе сообщения "Задач пока нет..."
- **Автоматически**: Работает для `.todo__empty-message`

---

### 8. **Анимация фокуса на инпуте**
- **Эффект**: Мягкое свечение вокруг поля ввода
- **Когда использовать**: При фокусе на поле поиска/ввода
- **Автоматически**: Работает при `:focus`

---

### 9. **Анимация кнопки удаления**
- **Вариант 1**: Пульсация (спокойный вариант)
- **Вариант 2**: Встряска (более заметный вариант)
- **Когда использовать**: При наведении на кнопку "Del"

**Выберите один вариант** - оба применены к `.delete-button:hover`, используйте только один!

---

### 10. **Улучшенная анимация popover**
- **Эффект**: Появление с верхнего края с легким отскоком
- **Когда использовать**: При открытии модального окна добавления задачи
- **Автоматически**: Работает через `:popover-open`

**Бонус**: Размытие фона с градиентом

---

### 11. **Skeleton загрузка**
- **Эффект**: Эффект бегущей полосы (shimmer)
- **Когда использовать**: При загрузке данных
- **Требует HTML**: Добавьте класс `.skeleton` к элементу

**Пример**:
```html
<div class="skeleton" style="height: 40px; width: 100%;"></div>
```

---

### 12. **Анимация кнопки "+"**
- **Эффект**: Вращение символа "+" на 90 градусов
- **Когда использовать**: При наведении на кнопку быстрого добавления
- **Автоматически**: Работает при `:hover`

---

### 13. **Анимация зачеркивания текста**
- **Эффект**: Линия появляется слева направо
- **Когда использовать**: При отметке задачи как выполненной
- **Автоматически**: Работает для `.complete-line`

---

### 14. **Подсветка новой задачи**
- **Эффект**: Зеленая подсветка и легкое увеличение
- **Когда использовать**: При добавлении новой задачи
- **Требует JavaScript**: Добавьте класс `.new-item` на 0.5 секунды

---

### 15. **Поддержка prefers-reduced-motion**
- **Что делает**: Отключает все анимации для пользователей с настройкой "Уменьшить движение"
- **Автоматически**: Работает через медиа-запрос

---

## 🔧 Интеграция в проект

### Вариант 1: Минимальный (только основные анимации)

Используйте эти анимации:
- ✅ Появление задач (#1)
- ✅ Раскрытие описания (#3)
- ✅ Анимация чекбокса (#4)
- ✅ Анимация фильтров (#5)
- ✅ Анимация фокуса (#8)

### Вариант 2: Стандартный (рекомендуемый)

Добавьте к минимальному:
- ✅ Удаление задач (#2) - требует JS
- ✅ Анимация пустого состояния (#7)
- ✅ Улучшенная анимация popover (#10)
- ✅ Анимация кнопки "+" (#12)

### Вариант 3: Максимальный (все анимации)

Используйте все анимации, включая:
- ✅ Счетчики (#6) - требует JS
- ✅ Кнопка удаления (#9)
- ✅ Зачеркивание текста (#13)
- ✅ Подсветка новой задачи (#14)

---

## 💻 Примеры использования в JavaScript

### Пример 1: Анимация удаления задачи

```javascript
// В actions/removeToDo.js
const removeToDo = (taskId) => {
  const taskWrapper = document.querySelector(`[data-js-task-id="${taskId}"]`);
  
  if (taskWrapper) {
    const todoItem = taskWrapper.closest('.todo__item');
    todoItem.classList.add('removing');
    
    // Удаляем после анимации
    setTimeout(() => {
      const newToDos = state.todos.filter((todo) => todo.id !== taskId);
      if (newToDos.length !== state.todos.length) {
        state.todos = newToDos;
        notify();
      }
    }, 300);
  } else {
    // Fallback если элемент не найден
    const newToDos = state.todos.filter((todo) => todo.id !== taskId);
    if (newToDos.length !== state.todos.length) {
      state.todos = newToDos;
      notify();
    }
  }
}
```

### Пример 2: Подсветка новой задачи

```javascript
// В actions/addToDo.js после notify()
const highlightNewTask = (taskId) => {
  setTimeout(() => {
    const taskWrapper = document.querySelector(`[data-js-task-id="${taskId}"]`);
    if (taskWrapper) {
      const todoItem = taskWrapper.closest('.todo__item');
      todoItem.classList.add('new-item');
      
      setTimeout(() => {
        todoItem.classList.remove('new-item');
      }, 500);
    }
  }, 50); // Небольшая задержка для рендера
};
```

### Пример 3: Анимация счетчиков без первого рендера

```javascript
// В ui/renderCounters.js
let isFirstRender = true;

const renderCounters = () => {
  const tasksCountAll = document.querySelector('[data-js-tasks-count]')
  const tasksCountActive = document.querySelector('[data-js-active-tasks-count]')
  const tasksCountComplete = document.querySelector('[data-js-complete-tasks-count]')

  const allTasksCount = state.todos.length
  const activeTasksCount = state.todos.filter(todo => todo.taskStatus === 'active').length
  const completeTasksCount = state.todos.filter(todo => todo.taskStatus === 'complete').length

  // Отключаем анимацию на первом рендере
  if (isFirstRender) {
    [tasksCountAll, tasksCountActive, tasksCountComplete].forEach(el => {
      if (el) el.classList.add('no-animation');
    });
    isFirstRender = false;
  }

  if (tasksCountAll) {
    tasksCountAll.textContent = allTasksCount;
    tasksCountAll.classList.remove('no-animation');
  }
  if (tasksCountActive) {
    tasksCountActive.textContent = activeTasksCount;
    tasksCountActive.classList.remove('no-animation');
  }
  if (tasksCountComplete) {
    tasksCountComplete.textContent = completeTasksCount;
    tasksCountComplete.classList.remove('no-animation');
  }
}
```

---

## 🎨 Настройка параметров

Все основные параметры вынесены в переменные в начале файла `animations.css`:

```css
--animation-fast: 0.15s;    /* Быстрые анимации */
--animation-normal: 0.3s;   /* Стандартные анимации */
--animation-slow: 0.5s;     /* Медленные анимации */

--easing-smooth: cubic-bezier(0.4, 0, 0.2, 1);      /* Плавная */
--easing-bounce: cubic-bezier(0.68, -0.55, 0.265, 1.55);  /* С отскоком */
--easing-elastic: cubic-bezier(0.175, 0.885, 0.32, 1.275); /* Эластичная */
```

Измените эти значения для настройки скорости и стиля всех анимаций!

---

## ⚠️ Важные замечания

1. **Производительность**: Все анимации используют `transform` и `opacity` для лучшей производительности
2. **Доступность**: Анимации автоматически отключаются для пользователей с `prefers-reduced-motion`
3. **Совместимость**: Используйте только CSS-анимации, не требующие JS, для базовых эффектов
4. **Тестирование**: Проверьте анимации на мобильных устройствах - некоторые могут быть слишком интенсивными

---

## 🚫 Отключение конкретных анимаций

Если какая-то анимация вам не нравится, просто закомментируйте соответствующий блок в `animations.css` или удалите его.

---

## 📱 Рекомендации для мобильных

На мобильных устройствах можно сделать анимации быстрее:

```css
@media (max-width: 600px) {
  :root {
    --animation-fast: 0.1s;
    --animation-normal: 0.2s;
    --animation-slow: 0.3s;
  }
}
```
